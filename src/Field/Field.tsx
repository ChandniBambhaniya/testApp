import React from 'react';
import {Text, View} from 'react-native';

const Field = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{fontSize: 18, color: '#000'}}>Field</Text>
    </View>
  );
};

export default Field;
